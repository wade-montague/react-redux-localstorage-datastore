import ACTION from "./action";
import MIDDLEWARE from "./middleware";
import REDUCER from "./reducer";
import TYPE from "./type";

const UI = { ACTION, MIDDLEWARE, REDUCER, TYPE };

export default UI;
