const ENTITY = "[ui]";
const TYPE = {
  INCREMENT_ACTION_COUNTER: `${ENTITY} Update action counter`,
  INCREMENT_PRIORITY_ACTION_COUNTER: `${ENTITY} Update priority action counter`
};

export default TYPE;
export { ENTITY };
